fn main() {
    let coord = (2,3);
    println!("{:?}, {:?}", coord.0, coord.1);

  //destructuring
    let (x, y) = (2, 3);
    println!("{:?}, {:?}", x, y);

  //mix and match data types
    let (name, age) = ("Shadow", 20);
}